<xml>
	<!-- TO EDIT THE NAME OF THE NDLL EDIT THIS -->
	<set name="NDLL_NAME" value="cne-mods-lua"/>

	<include name="${HXCPP}/build-tool/BuildCommon.xml"/>

	<files id="common">
		<compilerflag value="-Ilua"/>
		<compilerflag value="-Iinclude"/>

		<file name="common/ExternalInterface.cpp"/>
		<file name="common/Utils.cpp"/>
	</files>

	<set name="SLIBEXT" value=".lib" if="windows"/>
	<set name="SLIBEXT" value=".a" unless="windows"/>
	<set name="SLIBEXT" value=".so" if="webos"/>

	<set name="CNE_EXT" value="-windows" if="windows"/>
	<set name="CNE_EXT" value="-mac" if="mac"/>
	<set name="CNE_EXT" value="-linux" if="linux"/>
	<set name="CNE_EXT" value="-android" if="android"/>
	<set name="CNE_EXT" value="-ios" if="iphoneos"/>

	<!-- prefix = ${LIBPREFIX} -->
	<!-- suffix = ${MSVC_LIB_VERSION}${DEBUGEXTRA}${LIBEXTRA} -->
	<target id="NDLL" output="${NDLL_NAME}${CNE_EXT}" tool="linker" toolid="${STD_MODULE_LINK}">
		<outdir name="../ndll/${BINDIR}"/>
		<ext value=".ndll" if="windows || mac || linux"/>
		<files id="common"/>

		<section if="windows">
            <lib name='lua/lib/Windows64/lua51.lib' if='HXCPP_M64' />
            <lib name='lua/lib/Windows/lua51.lib' if='HXCPP_M32' />
        </section>
        <section if="linux">
            <lib name='lua/lib/Linux64/libluajit.so' if='HXCPP_M64' />
            <!-- <lib name='/lua/lib/Linux/libluajit.so' if='HXCPP_M32' /> -->
        </section>
        <section if="mac">
            <lib name='lua/lib/Mac/libluajit-5.1.a' if='HXCPP_M64' />
            <!-- <lib name='/lua/lib/Mac/libluajit-5.1.a' if='HXCPP_M32' /> -->
        </section>
        <section if="android">
            <lib name='lua/Android/libluajit-v7.so' if='HXCPP_ARMV7' />
            <lib name='lua/Android/libluajit-x86.so' if='HXCPP_X86' />
            <lib name='lua/Android/libluajit-64.so' if='HXCPP_ARM64' />
        </section>
	</target>

	<target id="default">
		<target id="NDLL"/>
	</target>
</xml>